#These dont work.  Need to refactor to a script that copies from the vms to the host then updates each node with the other keys.

#ssh-copy-id -i ~/.ssh/id_rsa.pub vagrant@master
#ssh-copy-id -i ~/.ssh/id_rsa.pub vagrant@master2
#ssh-copy-id -i ~/.ssh/id_rsa.pub vagrant@master3
